{
  "name": "odicto",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev -p 1420",
    "build": "NODE_OPTIONS=--max_old_space_size=4096 next build && next export -o dist",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.2.0",
    "next": "^13.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.2.2"
  }
}
